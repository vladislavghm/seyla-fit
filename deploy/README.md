# Деплой Seyla Fit на Beget VPS

Этот набор скриптов автоматизирует установку и деплой проекта Seyla Fit на VPS сервер Beget.

## 📋 Быстрый старт

### 1. Подготовка VPS на Beget

1. Зайдите в панель Beget → **VPS**
2. Создайте новый VPS (рекомендуется **VPS Pro** - от 300₽/мес)
3. Выберите ОС: **Ubuntu 22.04 LTS** или **Debian 12**
4. Дождитесь создания сервера и получите данные для SSH доступа

### 2. Подключение к серверу

```bash
ssh username@ваш-ip-адрес
```

### 3. Загрузка скриптов установки

**Вариант А: Если проект уже в Git репозитории (рекомендуется)**

```bash
cd ~
git clone https://github.com/leonov-xi/seyla-fit.git
cd seyla-fit
```

**Вариант Б: Загрузить только скрипты**

```bash
cd ~
mkdir -p seyla-fit/deploy
# Скопируйте файлы install.sh, ecosystem.config.js, deploy.sh в директорию deploy/
cd seyla-fit
```

### 4. Запуск автоматической установки

```bash
cd ~/seyla-fit
chmod +x deploy/install.sh
bash deploy/install.sh
```

Скрипт автоматически:

- ✅ Установит Node.js 20.x, pnpm, PM2, Nginx, Certbot
- ✅ Склонирует/обновит репозиторий
- ✅ Установит зависимости проекта
- ✅ Запросит переменные окружения для TinaCMS
- ✅ Соберёт проект
- ✅ Настроит PM2 для автозапуска
- ✅ Настроит Nginx с SSL сертификатом

**Во время установки вам нужно будет:**

- Ввести переменные окружения TinaCMS (или оставить пустым и добавить позже)
- Ввести email для SSL сертификата

### 5. Настройка DNS на Beget

1. Зайдите в панель Beget → **Домены**
2. Найдите домен **seyla-fit.ru** → **DNS настройки**
3. Добавьте A-записи:
   ```
   @       A       ваш-ip-адрес-сервера
   www     A       ваш-ip-адрес-сервера
   ```
4. Сохраните изменения
5. Подождите распространения DNS (5-30 минут)

### 6. Проверка работы

После настройки DNS проверьте:

```bash
# Статус приложения
pm2 status

# Логи приложения
pm2 logs seyla-fit

# Проверка Nginx
sudo systemctl status nginx
```

Откройте в браузере:

- https://seyla-fit.ru - главная страница
- https://seyla-fit.ru/admin - админка TinaCMS

## 🔄 Деплой обновлений

### Автоматический деплой (рекомендуется)

После изменений контента через админку TinaCMS, изменения автоматически попадают в Git.
Для автоматического обновления сайта настройте GitHub Webhook:

1. **GitHub → Repository → Settings → Webhooks → Add webhook**
2. **Payload URL:** `https://seyla-fit.ru/webhook`
3. **Content type:** `application/json`
4. **Secret:** ваш `WEBHOOK_SECRET` из `.env.production` (если не задан, оставьте пустым)
5. **Events:** выберите "Just the push event"
6. **Active:** ✓

После настройки каждое изменение в Git автоматически запустит деплой.

### Ручной деплой

Если webhook не настроен, запустите деплой вручную после изменений:

```bash
cd ~/seyla-fit
bash deploy/deploy.sh
```

Или пошагово:

```bash
cd ~/seyla-fit
git pull
pnpm install
pnpm build
pm2 restart seyla-fit
```

## ⚙️ Настройка переменных окружения

Если нужно изменить переменные окружения:

```bash
cd ~/seyla-fit
nano .env.production
```

Добавьте/измените:

```env
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_TINA_CLIENT_ID=ваш_client_id
TINA_TOKEN=ваш_token
NEXT_PUBLIC_TINA_BRANCH=main
```

После изменения перезапустите приложение:

```bash
pm2 restart seyla-fit
```

## 🔄 Смена аккаунта TinaCMS или репозитория

### Смена аккаунта TinaCMS

Если нужно переключиться на другой аккаунт TinaCMS:

1. **Получите новые ключи:**

   - Зайдите в [Tina Cloud Dashboard](https://app.tina.io)
   - Создайте новый проект или войдите в существующий
   - Получите новые `NEXT_PUBLIC_TINA_CLIENT_ID` и `TINA_TOKEN`

2. **Обновите `.env.production`:**

   ```bash
   cd ~/seyla-fit
   nano .env.production
   ```

   Измените значения:

   ```env
   NEXT_PUBLIC_TINA_CLIENT_ID=новый_client_id
   TINA_TOKEN=новый_token
   NEXT_PUBLIC_TINA_BRANCH=main  # или другой branch, если нужно
   ```

3. **Перезапустите приложение:**

   ```bash
   pm2 restart seyla-fit
   ```

4. **Проверьте работу:**
   - Откройте https://seyla-fit.ru/admin
   - Должна открыться админка с новым аккаунтом

### Смена GitHub репозитория

Если нужно переключиться на другой репозиторий:

1. **Измените remote URL:**

   ```bash
   cd ~/seyla-fit

   # Проверьте текущий remote
   git remote -v

   # Измените на новый репозиторий (SSH)
   git remote set-url origin git@github.com:новый-пользователь/новый-репозиторий.git

   # Или HTTPS (потребует настройки токена)
   git remote set-url origin https://github.com/новый-пользователь/новый-репозиторий.git
   ```

2. **Если меняется владелец репозитория (другой GitHub аккаунт):**

   **Вариант А: Использовать существующий SSH ключ (если у вас есть доступ)**

   ```bash
   # Проверьте SSH ключ
   ssh -T git@github.com
   ```

   **Вариант Б: Добавить новый SSH ключ для нового аккаунта**

   ```bash
   # Создайте новый SSH ключ
   ssh-keygen -t ed25519 -f ~/.ssh/id_github_new -N ""

   # Покажите публичный ключ
   cat ~/.ssh/id_github_new.pub
   ```

   Добавьте ключ в новый GitHub аккаунт:

   - GitHub → Settings → SSH and GPG keys → New SSH key

   Настройте SSH config для использования разных ключей:

   ```bash
   nano ~/.ssh/config
   ```

   Добавьте:

   ```
   Host github.com
       HostName github.com
       User git
       IdentityFile ~/.ssh/id_ed25519

   Host github-new
       HostName github.com
       User git
       IdentityFile ~/.ssh/id_github_new
   ```

   Затем используйте:

   ```bash
   git remote set-url origin git@github-new:новый-пользователь/новый-репозиторий.git
   ```

3. **Проверьте подключение:**

   ```bash
   git fetch origin
   git pull origin main
   ```

4. **Обновите webhook в GitHub:**

   - Зайдите в новый репозиторий: Settings → Webhooks
   - Если webhook уже настроен - проверьте, что URL правильный: `https://seyla-fit.ru/webhook`
   - Если webhook нет - создайте новый (см. раздел "Автоматический деплой")

5. **Перезапустите приложение (для применения изменений):**
   ```bash
   pm2 restart seyla-fit
   ```

**Примечание:** Если меняется репозиторий, но остаётся тот же GitHub аккаунт, достаточно изменить только remote URL (шаг 1).

## 🛠 Полезные команды

```bash
# Статус приложения
pm2 status

# Просмотр логов
pm2 logs seyla-fit

# Перезапуск приложения
pm2 restart seyla-fit

# Остановка приложения
pm2 stop seyla-fit

# Мониторинг ресурсов
pm2 monit

# Перезагрузка Nginx
sudo systemctl reload nginx

# Проверка конфигурации Nginx
sudo nginx -t

# Просмотр логов Nginx
sudo tail -f /var/log/nginx/seyla-fit-access.log
sudo tail -f /var/log/nginx/seyla-fit-error.log
```

## 🔒 Получение SSL сертификата вручную

Если автоматическое получение SSL не сработало:

```bash
sudo certbot --nginx -d seyla-fit.ru -d www.seyla-fit.ru
```

## 📁 Структура файлов

```
deploy/
  ├── install.sh           # Автоматическая установка всего
  ├── deploy.sh            # Скрипт быстрого деплоя обновлений
  ├── ecosystem.config.js  # Конфигурация PM2
  └── README.md           # Эта инструкция
```

## ⚠️ Важные замечания

1. **Переменные окружения** - обязательно настройте `.env.production` перед первым запуском
2. **DNS** - настройте A-записи на Beget перед получением SSL сертификата
3. **Порты** - убедитесь, что порт 3000 не занят другими приложениями
4. **Безопасность** - храните `.env.production` в безопасности, не коммитьте в Git

## 🐛 Решение проблем

### Приложение не запускается

```bash
pm2 logs seyla-fit --lines 50
```

### Ошибка "TINA_TOKEN is invalid"

Проверьте переменные окружения:

```bash
cd ~/seyla-fit
cat .env.production
pm2 restart seyla-fit
```

### SSL сертификат не работает

**Автоматическая переустановка:**

```bash
cd ~/seyla-fit
bash deploy/setup-ssl.sh
```

**Или вручную:**

Проверьте DNS записи и попробуйте получить сертификат вручную:

```bash
sudo certbot --nginx -d seyla-fit.ru -d www.seyla-fit.ru
```

**Примечание:** После переустановки SSL может потребоваться восстановить конфигурацию `/webhook` (см. раздел "GitHub Webhook возвращает 404").

### Nginx возвращает 502 Bad Gateway

Проверьте, что приложение запущено:

```bash
pm2 status
curl http://localhost:3000
```

### GitHub Webhook возвращает 404

Если GitHub webhook показывает ошибку 404, это означает, что конфигурация `/webhook` отсутствует в Nginx (возможно, её удалил certbot).

**Автоматическое исправление:**

```bash
cd ~/seyla-fit
bash deploy/fix-webhook.sh
```

**Или вручную:**

1. Откройте конфигурацию Nginx:

```bash
sudo nano /etc/nginx/sites-available/seyla-fit
```

2. Найдите блок `server` с `listen 443` и добавьте перед последней `}`:

```nginx
    # GitHub Webhook для автоматического деплоя
    location /webhook {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-GitHub-Event $http_x_github_event;
        proxy_set_header X-Hub-Signature-256 $http_x_hub_signature_256;
        proxy_read_timeout 300;
        proxy_connect_timeout 300;
    }
```

3. Проверьте и перезагрузите:

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 📞 Помощь

- [Документация TinaCMS](https://tina.io/docs/)
- [Документация Next.js](https://nextjs.org/docs)
- [Документация PM2](https://pm2.keymetrics.io/docs/)
- [Документация Nginx](https://nginx.org/ru/docs/)
