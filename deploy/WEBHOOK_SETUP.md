# 🔗 Настройка автоматического деплоя через GitHub Webhook

Эта инструкция поможет настроить автоматический деплой при каждом коммите в Git (включая коммиты от TinaCMS).

## Как это работает:

1. **TinaCMS редактирует контент** → делает коммит в Git
2. **GitHub отправляет webhook** на ваш сервер
3. **Webhook сервер** автоматически запускает `deploy.sh`
4. **Сайт обновляется** автоматически

## Настройка (после установки на сервер):

### Шаг 1: Получите секретный ключ для webhook

Сгенерируйте случайный секретный ключ на сервере:

```bash
ssh username@ваш-ip-адрес
cd ~/seyla-fit
openssl rand -hex 32
```

Скопируйте полученный ключ (например: `a1b2c3d4e5f6...`)

### Шаг 2: Добавьте секрет в переменные окружения

Отредактируйте `.env.production`:

```bash
nano ~/seyla-fit/.env.production
```

Добавьте строку (используйте ключ из шага 1):

```env
WEBHOOK_SECRET=ваш_сгенерированный_ключ
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X).

### Шаг 3: Перезапустите webhook сервер

```bash
cd ~/seyla-fit
pm2 restart webhook-server
# Или если webhook еще не запущен:
pm2 restart all
```

### Шаг 4: Настройте webhook в GitHub

1. Откройте ваш репозиторий на GitHub: `https://github.com/leonov-xi/seyla-fit`

2. Перейдите в **Settings** → **Webhooks** → **Add webhook**

3. Заполните форму:

   - **Payload URL**: `https://seyla-fit.ru/webhook`
   - **Content type**: `application/json`
   - **Secret**: вставьте тот же ключ, что использовали в `.env.production`
   - **Which events**: выберите "Just the push event"
   - **Active**: убедитесь, что галочка стоит

4. Нажмите **"Add webhook"**

### Шаг 5: Проверьте работу

1. Проверьте статус webhook сервера:

   ```bash
   pm2 status
   pm2 logs webhook-server
   ```

2. Сделайте тестовый коммит в репозиторий (или отредактируйте контент через TinaCMS админку)

3. Проверьте логи:

   ```bash
   pm2 logs webhook-server --lines 50
   ```

4. Проверьте, что сайт обновился автоматически

## Проверка webhook на GitHub

В GitHub зайдите в **Settings** → **Webhooks** → нажмите на ваш webhook. Там вы увидите:

- ✅ Зеленые галочки = webhook работает
- ❌ Красные крестики = есть проблемы (проверьте логи)

## Устранение проблем

### Webhook не срабатывает

1. Проверьте, что webhook сервер запущен:

   ```bash
   pm2 status webhook-server
   ```

2. Проверьте логи:

   ```bash
   pm2 logs webhook-server
   ```

3. Проверьте, что порт 9000 доступен:

   ```bash
   curl http://localhost:9000/webhook
   ```

4. Проверьте настройки Nginx:
   ```bash
   sudo nginx -t
   sudo systemctl status nginx
   ```

### Webhook возвращает 401 (Invalid signature)

Убедитесь, что `WEBHOOK_SECRET` в `.env.production` совпадает с Secret в настройках GitHub webhook.

### Изменения не применяются

Проверьте логи деплоя:

```bash
pm2 logs seyla-fit
cat ~/seyla-fit/logs/webhook.log
```

## Отключение автоматического деплоя

Если нужно временно отключить автоматический деплой:

```bash
pm2 stop webhook-server
```

Чтобы включить обратно:

```bash
pm2 start webhook-server
```

## Ручной деплой (если webhook не работает)

Вы всегда можете запустить деплой вручную:

```bash
cd ~/seyla-fit
bash deploy/deploy.sh
```

---

**Готово!** Теперь каждый раз, когда TinaCMS делает коммит (или вы пушите изменения в Git), сайт будет автоматически обновляться. 🚀
