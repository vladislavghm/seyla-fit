# 🚀 Быстрый старт - Деплой на Beget

## Что нужно сделать (пошагово):

### 1️⃣ Создайте VPS на Beget

- Зайдите в панель Beget → **VPS**
- Создайте **VPS Pro** (от 300₽/мес)
- Выберите ОС: **Ubuntu 22.04 LTS**
- Запишите IP адрес сервера

### 2️⃣ Подключитесь к серверу

```bash
ssh username@ваш-ip-адрес
```

### 3️⃣ Запустите автоматическую установку

```bash
# Клонируем проект
cd ~
git clone https://github.com/leonov-xi/seyla-fit.git
cd seyla-fit

# Запускаем установку
chmod +x deploy/install.sh
bash deploy/install.sh
```

### 4️⃣ Во время установки:

- ✅ Введите переменные окружения TinaCMS (или оставьте пустым)
- ✅ Введите email для SSL сертификата

### 5️⃣ Настройте DNS на Beget

1. Панель Beget → **Домены** → **seyla-fit.ru** → **DNS настройки**
2. Добавьте A-записи:
   ```
   @       A       ваш-ip-адрес-сервера
   www     A       ваш-ip-адрес-сервера
   ```
3. Сохраните и подождите 5-30 минут

### 6️⃣ Настройте автоматический деплой (опционально)

Для автоматического обновления сайта при редактировании через админку:

1. Следуйте инструкции: `deploy/WEBHOOK_SETUP.md`
2. Настройте GitHub webhook (займет 2-3 минуты)

Или запускайте деплой вручную:
```bash
cd ~/seyla-fit
bash deploy/deploy.sh
```

### 7️⃣ Готово! 🎉

Откройте в браузере:

- https://seyla-fit.ru
- https://seyla-fit.ru/admin

---

## 📝 Если нужно добавить/изменить переменные окружения:

```bash
cd ~/seyla-fit
nano .env.production
# Добавьте/измените переменные:
# NEXT_PUBLIC_TINA_CLIENT_ID=ваш_client_id
# TINA_TOKEN=ваш_token
# NEXT_PUBLIC_TINA_BRANCH=main

# Перезапустите приложение
pm2 restart seyla-fit
```

## 🔄 Деплой обновлений:

```bash
cd ~/seyla-fit
bash deploy/deploy.sh
```

## 📊 Проверка статуса:

```bash
pm2 status              # Статус приложения
pm2 logs seyla-fit      # Логи приложения
```

---
