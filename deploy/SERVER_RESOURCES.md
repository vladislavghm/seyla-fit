# 📊 Расчет ресурсов сервера для Next.js + TinaCMS

## Текущая ситуация

- **Next.js сборка**: требует ~1-1.5GB RAM ✅
- **Запущенное приложение**: требует ~200-500MB RAM ✅
- **TinaCMS build (генерация админки)**: требует ~3-4GB RAM ❌

## Рекомендации по конфигурации сервера

### Минимальная конфигурация (для текущего проекта)

**Рекомендуется: 4GB RAM**

- ✅ Достаточно для сборки Next.js
- ✅ Достаточно для работы приложения
- ✅ Достаточно для одновременной работы приложения + webhook-server
- ❌ **НЕ достаточно для TinaCMS build** (но это решается генерацией админки локально)

**Стоимость на Beget**: ~22-25₽/день (~660-750₽/месяц)

### Оптимальная конфигурация (для полной автоматизации)

**Рекомендуется: 6-8GB RAM**

- ✅ Достаточно для TinaCMS build на сервере
- ✅ Полная автоматизация всех процессов
- ✅ Запас для будущего роста

**Стоимость на Beget**: ~35-45₽/день (~1050-1350₽/месяц)

### Минимальная (экономичная) конфигурация

**Рекомендуется: 3GB RAM**

- ✅ Достаточно для сборки Next.js
- ✅ Достаточно для работы приложения
- ❌ Требует генерации админки локально (но это делается редко)

**Стоимость на Beget**: ~17-20₽/день (~510-600₽/месяц)

## Формула расчета RAM

```
Требуемая RAM = 
  + Система (Ubuntu): 500MB-1GB
  + Next.js сборка: 1-1.5GB
  + Запущенное приложение: 200-500MB
  + Webhook-server: 50-100MB
  + Запас (20-30%): +500MB-1GB
  + TinaCMS build (если нужен): +3-4GB
```

**Для обычной работы (без TinaCMS build):**
- Минимум: 2GB
- Рекомендуется: 3-4GB
- Комфортно: 4GB

**С TinaCMS build на сервере:**
- Минимум: 6GB
- Рекомендуется: 8GB
- Комфортно: 8-16GB

## Рекомендации для вашего проекта

### Вариант 1: Оставить 4GB RAM (РЕКОМЕНДУЕТСЯ) ⭐

**Плюсы:**
- Экономия: ~660-750₽/месяц
- Достаточно для всех операций, кроме TinaCMS build
- Админка обновляется редко (раз в несколько месяцев)

**Минусы:**
- Админку нужно генерировать локально

**Процесс:**
1. Обычный деплой: `bash deploy/deploy.sh` (работает автоматически)
2. Обновление админки (редко): `.\deploy\upload-admin.ps1` (локально)

### Вариант 2: Увеличить до 6-8GB RAM

**Плюсы:**
- Полная автоматизация
- TinaCMS build работает на сервере

**Минусы:**
- Дороже: ~1050-1350₽/месяц
- Избыточно для обычной работы

## Диск

- **Минимум**: 15-20GB
- **Рекомендуется**: 25-30GB
- **Для TinaCMS build**: +5-10GB (временные файлы)

## CPU

- **Минимум**: 1 ядро
- **Рекомендуется**: 2 ядра (для параллельных операций)
- **Для TinaCMS build**: 2 ядра желательны

## Вывод

**Для вашего проекта оптимально: 4GB RAM**

Админка TinaCMS обновляется очень редко (только при изменении схемы контента), поэтому генерация локально - разумное решение. Это сэкономит ~300-600₽/месяц, а ручная операция потребуется раз в несколько месяцев.

Если проект будет расти, можно увеличить до 6GB позже.

